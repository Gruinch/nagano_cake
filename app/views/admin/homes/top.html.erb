<div class="container">
  <div class="row">
    <h5>注文履歴一覧</h5>
  </div>
  <div class="row">
  <div class="col-2"></div>
  <div class="col-8">
    <table class="table">
      <thead>
        <tr>
          <th scope="col">購入日時</th>
          <th scope="col">購入者</th>
          <th scope="col">注文個数</th>
          <th scope="col">注文ステータス</th>
        </tr>
      </thead>
      <tbody>
        <% @orders.order_details.each do |orderDetail| %>
        <tr>
          <td><%= link_to order.created_at, admin_order_path(@order) %></td>
          <td><%= customer.name  %></td>
          <td><%= order.amount  %></td>
          <td><%= order.order_status %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  </div>
</div>